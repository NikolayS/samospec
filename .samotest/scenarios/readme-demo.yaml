id: readme-demo
title: README spec-creation demo GIF
owner: "@samospec"
tags: ["docs", "demo", "gif", "spec-creation"]
priority: required
target:
  url: "http://127.0.0.1:43731/docs/demo.html"
recording:
  mode: browser
  duration_ms: 4500
  output_path: docs/demo.gif
  preferred_formats: ["gif"]
steps:
  - id: capture-spec-creation-demo
    instruction: "Record the README demo page built from a real concise spec-creation run."
    expected: "The GIF shows init/new output and the generated SPEC.md excerpt."
    evidence:
      - type: gif
        name: readme-demo
result:
  required_observations:
    - "The demo shows spec creation, not CLI help text."
